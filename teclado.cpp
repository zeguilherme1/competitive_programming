#include <bits/stdc++.h>
using namespace std;


int main(){
	ios::sync_with_stdio(false);
	int t;
	cin >> t;
	while(t--){
		string s;
		cin >> s;
	
		string teste;
		cin >> teste;
		
		for(int i = 0; i < teste.size(); i++){
			for(int j = 0; j < s.size(); j++){
				if(teste[i] == s[j]){
					
				}
			}
		}
	}

	return 0;
}
